# Design system

Keep durable tokens in `tokens/`, small reusable primitives in `primitives/`, and interaction patterns in `motion/`. Motion must use shared tokens and respect the system Reduce Motion preference.
