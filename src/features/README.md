# Features

Feature folders own user-facing use cases and screens. Routes only compose feature screens; features talk to repository/native/sync boundaries through explicit application contracts as those capabilities are implemented.
