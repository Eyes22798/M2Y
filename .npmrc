# React Native native builds still traverse node_modules from CMake/Prefab on
# Windows. A hoisted layout keeps generated native paths below the legacy
# MAX_PATH boundary used by Ninja 1.10 bundled with Android CMake 3.22.
node-linker=hoisted
